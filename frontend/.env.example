# FlowGuard AI - フロントエンド環境変数（サンプル）
# このファイルを .env にコピーし、値を設定してください。.env は Git にコミットしません。

# バックエンド API のベース URL（開発時は http://localhost:8000）
VITE_API_BASE_URL=http://localhost:8000

# Google Maps API キー（Maps JavaScript API を有効にしたキー）
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Google Map ID（3D 建物表示用。任意。Map ID を使用する場合のみ）
# VITE_GOOGLE_MAP_ID=your-map-id

# シミュレーションタイムアウト（ミリ秒）。任意。
# VITE_SIMULATE_TIMEOUT_MS=240000

# --- プロジェクト共有機能（Firebase）を使う場合のみ設定 ---
# 未設定の場合は「新規作成」「参加コードで参加」は利用できません。
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=
