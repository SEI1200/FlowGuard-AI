# FlowGuard AI - バックエンド環境変数（サンプル）
# このファイルを .env にコピーし、値を設定してください。.env は Git にコミットしません。

# Google Cloud プロジェクト ID（Vertex AI 利用に必要）
PROJECT_ID=your-gcp-project-id

# Google Maps API キー（Maps JavaScript API を有効にしたキー）
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Vertex AI のリージョン（gemini-3-pro-preview は global のみ。他モデルは us-central1 等）
LOCATION=global

# 使用する Gemini モデル（解析用。例: gemini-3-pro-preview）
MODEL_ID=gemini-3-pro-preview

# シミュレーションタイムアウト（ミリ秒）。任意。未設定時は 240000（4 分）
# VITE_SIMULATE_TIMEOUT_MS=300000
